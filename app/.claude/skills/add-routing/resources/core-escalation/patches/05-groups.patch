diff --git a/src/cli/resources/groups.ts b/src/cli/resources/groups.ts
index f7007d1f..1ec4f68e 100644
--- a/src/cli/resources/groups.ts
+++ b/src/cli/resources/groups.ts
@@ -257,7 +257,7 @@ registerResource({
       access: 'approval',
       description:
         'Update container config scalar fields. Changes are saved but do NOT take effect until you run `ncl groups restart`. ' +
-        'Use --id <group-id> and any of: --provider, --model, --effort, --image-tag, --assistant-name, --max-messages-per-prompt, --cli-scope, --egress (open|host-only|none).',
+        'Use --id <group-id> and any of: --provider, --model, --effort, --image-tag, --assistant-name, --max-messages-per-prompt, --cli-scope, --egress (open|host-only|none), --fallback-provider <name>|none.',
       handler: async (args) => {
         const id = args.id as string;
         if (!id) throw new Error('--id is required');
@@ -275,6 +275,7 @@ registerResource({
             | 'max_messages_per_prompt'
             | 'cli_scope'
             | 'egress'
+            | 'fallback_provider'
           >
         > = {};
         if (args.provider !== undefined) updates.provider = args.provider as string;
@@ -292,6 +293,10 @@ registerResource({
           updates.cli_scope = scope;
         }
 
+        if (args['fallback-provider'] !== undefined || args.fallback_provider !== undefined) {
+          const fp = String(args['fallback-provider'] ?? args.fallback_provider);
+          updates.fallback_provider = fp === 'none' ? null : fp;
+        }
         if (args.egress !== undefined) {
           const egress = args.egress as string;
           if (!['open', 'host-only', 'none'].includes(egress)) {
