diff --git a/src/types.ts b/src/types.ts
index aaf3ef9d..011375fc 100644
--- a/src/types.ts
+++ b/src/types.ts
@@ -40,6 +40,7 @@ export interface ContainerConfigRow {
   additional_mounts: string; // JSON: AdditionalMountConfig[]
   cli_scope: string; // 'disabled' | 'group' | 'global'
   egress: string | null; // 'open' (null) | 'host-only' | 'none' — container network reach
+  fallback_provider: string | null; // §16c escalation target (e.g. 'claude'); NULL = no escalation
   learning?: string | null; // JSON: { autoTrigger?, autoKeep?, cooldownMinutes? } — see docs/webchat/learning-loop.md
   updated_at: string;
 }
