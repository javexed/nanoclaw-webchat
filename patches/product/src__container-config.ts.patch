diff --git a/src/container-config.ts b/src/container-config.ts
index f9b0d100..4557094e 100644
--- a/src/container-config.ts
+++ b/src/container-config.ts
@@ -14,6 +14,9 @@ import { resolveContainerConfigAugmentation } from './seam/spawn-hooks.js';
 
 import { DEFAULT_MODEL, FAST_MODE, GROUPS_DIR, TIMEZONE } from './config.js';
 import { getContainerConfig } from './db/container-configs.js';
+import { roomLearningMapForAgent } from './modules/learning/room-settings.js';
+import { getLearningMasterEnabled, getLearningClassifier } from './modules/learning/master.js';
+import { resolveLearningClassifier } from './container-runtime-extras.js';
 import { getAgentGroup } from './db/agent-groups.js';
 import { isValidTimezone } from './timezone.js';
 import { log } from './log.js';
@@ -69,6 +72,17 @@ export interface McpHttpServerConfig {
   /** See McpStdioServerConfig.plugin — same ownership marker. */
   plugin?: string;
   instructions?: string;
+  /** Tool allowlist — absent means every tool the server exposes. */
+  enabledTools?: string[];
+}
+
+export interface McpRemoteServerConfig {
+  type: 'http';
+  url: string;
+  headers?: Record<string, string>;
+  instructions?: string;
+  /** Tool allowlist — absent means every tool the server exposes. */
+  enabledTools?: string[];
 }
 
 export type McpServerConfig = McpStdioServerConfig | McpHttpServerConfig;
@@ -156,8 +170,19 @@ export function parseMcpServerConfig(input: Record<string, unknown>): McpServerC
       throw new Error('url must be a valid HTTP(S) URL', { cause: err });
     }
     const loopback = ['localhost', '127.0.0.1', '[::1]', 'host.docker.internal'].includes(parsed.hostname);
-    if (parsed.protocol !== 'https:' && !(parsed.protocol === 'http:' && loopback)) {
-      throw new Error('url must use HTTPS (plain HTTP is allowed only for localhost and host.docker.internal)');
+    // Fork widening: plain HTTP is also allowed for private-LAN and CGNAT
+    // (Tailscale) hosts. Self-hosted tool servers on the LAN or tailnet are the
+    // NORMAL remote-MCP shape here (the live install's only remote server is
+    // http://100.x via Tailscale), and sanitizeStoredMcpServers re-parses stored
+    // entries — an HTTPS-only rule would silently drop a working server at
+    // materialization, not just refuse new ones.
+    const privateLan = /^(10\.|192\.168\.|172\.(1[6-9]|2\d|3[01])\.|100\.(6[4-9]|[7-9]\d|1[01]\d|12[0-7])\.)/.test(
+      parsed.hostname,
+    );
+    if (parsed.protocol !== 'https:' && !(parsed.protocol === 'http:' && (loopback || privateLan))) {
+      throw new Error(
+        'url must use HTTPS (plain HTTP is allowed only for localhost, host.docker.internal, and private-LAN/tailnet hosts)',
+      );
     }
     if (parsed.username || parsed.password || parsed.hash) {
       throw new Error('url must not contain credentials or fragments; use the credential gateway');
@@ -260,6 +285,42 @@ export interface ContainerConfig {
   timezone?: string;
   /** Session isolation tier for the group's containers; absent = the composer's default ('container'). */
   runtimeTier?: 'container' | 'vm';
+  /** Network egress mode: 'open' (default) | 'host-only' | 'none'. */
+  egress?: string;
+  /**
+   * Deliver unwrapped agent prose to the originating room instead of dropping
+   * it as scratchpad. Contributed by an installed module (webchat sets it for
+   * ollama-backed groups, whose small models rarely emit the <message> envelope)
+   * via resolveContainerConfigAugmentation. Read by the agent-runner.
+   */
+  lenientOutput?: boolean;
+  /**
+   * Relax the system prompt for weak/local models: the Claude provider drops
+   * the heavy `claude_code` preset in favor of the plain persona/destinations
+   * instructions. Contributed by an installed module (webchat sets it for
+   * ollama-backed groups, alongside lenientOutput) via
+   * resolveContainerConfigAugmentation. Read by the agent-runner.
+   */
+  lenientPrompt?: boolean;
+  /**
+   * Learning-loop behavior (docs/webchat/learning-loop.md). Absent keys mean defaults:
+   * autoTrigger ON (a busy turn auto-runs the review; it only STAGES a draft),
+   * autoKeep OFF (auto-accepting self-written context is an owner-level opt-in),
+   * cooldownMinutes 30.
+   */
+  learning?: {
+    autoTrigger?: boolean;
+    autoKeep?: boolean;
+    cooldownMinutes?: number;
+    /**
+     * Per-room overrides, keyed "<channel_type>:<platform_id>" — the pair the
+     * agent-runner knows from its routing context. Composed at materialize
+     * time from learning_room_settings; room keys win over the top level.
+     */
+    rooms?: Record<string, { autoTrigger?: boolean; autoKeep?: boolean }>;
+    /** Classifier gate — small local model consulted before a review. */
+    classifier?: { url: string; model: string };
+  };
 }
 
 /**
@@ -384,6 +445,10 @@ export function configFromDb(row: ContainerConfigRow, group: AgentGroup): Contai
     ...speedFields(parseContainerSpeed(row.speed) ?? (FAST_MODE ? 'fast' : undefined)),
     timezone: row.timezone && isValidTimezone(row.timezone) ? row.timezone : undefined,
     runtimeTier: parseRuntimeTier(row.runtime_tier, group.name),
+    // Without this the column, the ncl flag and the container-runner branch all
+    // exist but never meet: buildContainerArgs reads undefined and every group
+    // spawns with open egress, whatever was configured.
+    egress: row.egress ?? undefined,
   };
 }
 
@@ -417,6 +482,34 @@ export async function materializeContainerJson(agentGroupId: string): Promise<Co
   const config = configFromDb(row, group);
   Object.assign(config, resolveContainerConfigAugmentation(agentGroupId)); // seam: module-contributed fields win
 
+  // Workspace MASTER switch (Settings → Features → Auto-learn). Off overrides
+  // agent AND room settings: the runner sees learning fully off, so no busy
+  // turn ever auto-reviews and no draft auto-keeps. On → per-room overrides
+  // ride the same learning blob (rooms win over the agent level; see
+  // src/modules/learning/room-settings.ts for the precedence contract).
+  if (await getLearningMasterEnabled()) {
+    const roomLearning = await roomLearningMapForAgent(agentGroupId);
+    if (Object.keys(roomLearning).length > 0) {
+      config.learning = { ...(config.learning ?? {}), rooms: await roomLearning };
+    }
+    // Classifier gate (docs/webchat/learning-loop.md): a small local model the runner
+    // consults before an expensive review. Precedence:
+    //   1. An explicit Settings override (Auto-learn → Classifier model), resolved
+    //      to a container-reachable url at pick time.
+    //   2. Else auto-default to the agent's OWN model when it runs on a local
+    //      endpoint (resolver contributed by the webchat module) — zero setup.
+    //   3. Else none → the runner uses the busy-turn heuristic (Claude agents,
+    //      which have no local endpoint to call, land here).
+    const clf = await getLearningClassifier();
+    const classifier =
+      clf.url && clf.model ? { url: clf.url, model: clf.model } : await resolveLearningClassifier(agentGroupId);
+    if (classifier) {
+      config.learning = { ...(config.learning ?? {}), classifier };
+    }
+  } else {
+    config.learning = { autoTrigger: false, autoKeep: false };
+  }
+
   const p = path.join(GROUPS_DIR, group.folder, 'container.json');
   const dir = path.dirname(p);
   if (!fs.existsSync(dir)) fs.mkdirSync(dir, { recursive: true });
