diff --git a/src/modules/self-mod/request.ts b/src/modules/self-mod/request.ts
index fee5be59..14942edf 100644
--- a/src/modules/self-mod/request.ts
+++ b/src/modules/self-mod/request.ts
@@ -164,8 +164,8 @@ export async function validateAddMcpServer(content: Record<string, unknown>, ses
     return false;
   }
 
-  const args = serverConfig.type === 'http' ? [] : (serverConfig.args ?? []);
-  const env = serverConfig.type === 'http' ? {} : (serverConfig.env ?? {});
+  const args = 'url' in serverConfig ? [] : (serverConfig.args ?? []);
+  const env = 'url' in serverConfig ? {} : (serverConfig.env ?? {});
 
   if (args.length > MAX_MCP_ARGS) {
     await notifyAgent(session, `add_mcp_server failed: max ${MAX_MCP_ARGS} args per server.`);
@@ -189,7 +189,7 @@ export async function requestAddMcpServerHold(content: Record<string, unknown>,
   const serverConfig = parseMcpServerConfig(content);
 
   let fields: string[];
-  if (serverConfig.type === 'http') {
+  if ('url' in serverConfig) {
     // Redact per path segment and per query value, never the origin — the
     // admin must always see the true destination host, and SECRET_VALUE_RE
     // is start-anchored so testing the whole URL would match nothing. A
