diff --git a/container/agent-runner/src/providers/mock.ts b/container/agent-runner/src/providers/mock.ts
index 562f1fbb..c222fd9e 100644
--- a/container/agent-runner/src/providers/mock.ts
+++ b/container/agent-runner/src/providers/mock.ts
@@ -10,13 +10,35 @@ import type { AgentProvider, AgentQuery, ProviderEvent, ProviderOptions, QueryIn
  * registerProviderContract, the same two-step path every provider uses; this
  * module does not import it.
  */
+/**
+ * A factory may return either a plain string (treated as result text, the
+ * common case) or a richer descriptor so tests can simulate the graceful-
+ * degradation paths:
+ *
+ *  - `{ result: string | null }` — emit a result with explicit (possibly null /
+ *    empty) text, then END the turn (mirrors the real SDK closing after its
+ *    result). `null`/empty drives the empty-turn safety net.
+ *  - `{ error: {...} }` — emit a terminal error event instead of a result.
+ *  - `{ throw: string }` — throw mid-stream (the SDK-died case).
+ *  - `{ silent: true }` — emit no result/error at all (pure silence).
+ */
+export type MockResponse =
+  | string
+  | { result: string | null }
+  | { error: { message: string; retryable?: boolean; classification?: string } }
+  | { throw: string }
+  | { silent: true };
+
 export class MockProvider implements AgentProvider {
-  private responseFactory: (prompt: string) => string;
+  /** Stands in for a full provider, so it describes the full toolset. */
+  readonly supportsMcpTools = true;
+
+  private responseFactory: (prompt: string, input: QueryInput) => MockResponse;
   private textFactory: ((prompt: string) => string[]) | undefined;
 
   constructor(
     _options: ProviderOptions = {},
-    responseFactory?: (prompt: string) => string,
+    responseFactory?: (prompt: string, input: QueryInput) => MockResponse,
     /**
      * Optional mid-turn text segments: returns the `text` events to emit
      * before the result for a given prompt (mirrors SDK assistant messages
@@ -30,8 +52,11 @@ export class MockProvider implements AgentProvider {
 
   registerMemorySessionHook(_hook: MemorySessionHookRegistration): void {}
 
-  isSessionInvalid(_err: unknown): boolean {
-    return false;
+  isSessionInvalid(err: unknown): boolean {
+    // Mirror the real providers' stale-session detection so tests can exercise
+    // the poll-loop's continuation self-heal.
+    const msg = err instanceof Error ? err.message : String(err);
+    return /no conversation found|session.*not found/i.test(msg);
   }
 
   query(input: QueryInput): AgentQuery {
@@ -44,14 +69,43 @@ export class MockProvider implements AgentProvider {
     // Mid-turn text segments (if configured) followed by the turn's result —
     // mirrors the SDK's assistant-message → result ordering. The result text
     // itself streams as the LAST text event first: the real SDK's result only
-    // repeats the final assistant text, which already streamed.
-    function* turnEvents(prompt: string): Generator<ProviderEvent> {
+    // repeats the final assistant text, which already streamed — that is the
+    // mid-turn-complete delivery this mock's contract declares.
+    function* turnEvents(prompt: string): Generator<ProviderEvent, boolean> {
       for (const text of textFactory?.(prompt) ?? []) {
         yield { type: 'text', text };
       }
-      const result = responseFactory(prompt);
-      if (result) yield { type: 'text', text: result };
-      yield { type: 'result', text: result };
+      const resp = responseFactory(prompt, input);
+      if (typeof resp === 'string') {
+        // Legacy bare-string form: streams as text (upstream's one-door
+        // contract) then results, and keeps the stream OPEN for follow-up
+        // pushes — the behaviour the existing integration tests rely on.
+        if (resp) yield { type: 'text', text: resp };
+        yield { type: 'result', text: resp };
+        return false;
+      }
+      if ('throw' in resp) {
+        throw new Error(resp.throw);
+      }
+      if ('error' in resp) {
+        yield {
+          type: 'error',
+          message: resp.error.message,
+          retryable: resp.error.retryable ?? false,
+          classification: resp.error.classification,
+        };
+        return true;
+      }
+      if ('silent' in resp) {
+        // Pure silence — no result, no error. The turn is over.
+        return true;
+      }
+      // The object `{ result }` form always ends the turn (mirrors the real SDK
+      // closing after its result). Streamed as text first only when non-empty:
+      // a null/empty result is exactly the empty-turn case under test.
+      if (resp.result) yield { type: 'text', text: resp.result };
+      yield { type: 'result', text: resp.result };
+      return true;
     }
 
     const events: AsyncIterable<ProviderEvent> = {
@@ -61,13 +115,13 @@ export class MockProvider implements AgentProvider {
 
         // Process initial prompt
         yield { type: 'activity' };
-        yield* turnEvents(input.prompt);
+        if (yield* turnEvents(input.prompt)) return;
 
         // Process any pushed follow-ups
         while (!ended && !aborted) {
           if (pending.length > 0) {
             const msg = pending.shift()!;
-            yield* turnEvents(msg);
+            if (yield* turnEvents(msg)) return;
             continue;
           }
           // Wait for push() or end()
@@ -80,7 +134,7 @@ export class MockProvider implements AgentProvider {
         // Drain remaining
         while (pending.length > 0) {
           const msg = pending.shift()!;
-          yield* turnEvents(msg);
+          if (yield* turnEvents(msg)) return;
         }
       },
     };
