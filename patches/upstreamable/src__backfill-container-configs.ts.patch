diff --git c/src/backfill-container-configs.ts w/src/backfill-container-configs.ts
index 12c6f38e..66ccc1a1 100644
--- c/src/backfill-container-configs.ts
+++ w/src/backfill-container-configs.ts
@@ -64,6 +64,7 @@ export function backfillContainerConfigs(): void {
       packages_apt: JSON.stringify(legacy.packages?.apt ?? []),
       packages_npm: JSON.stringify(legacy.packages?.npm ?? []),
       additional_mounts: JSON.stringify(legacy.additionalMounts ?? []),
+      egress: null,
       cli_scope: 'group',
       timezone: null,
       updated_at: new Date().toISOString(),
